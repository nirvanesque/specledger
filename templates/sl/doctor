#!/bin/bash
# SpecLedger: Check installation status of required and optional tools
# Usage: sl/doctor [options]

set -e

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the sl doctor command
exec sl doctor "$@"
