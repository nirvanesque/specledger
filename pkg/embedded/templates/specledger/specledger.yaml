# SpecLedger Project Metadata v1.0.0
# This file contains project configuration and dependency tracking
# DO NOT edit this file manually - use `sl deps` commands instead

version: "1.0.0"

project:
  name: "{{.ProjectName}}"
  short_code: "{{.ShortCode}}"
  created: "{{.Created}}"
  modified: "{{.Modified}}"
  version: "0.1.0"

framework:
  choice: "{{.FrameworkChoice}}"
  # installed_at is set automatically when frameworks are installed via mise

dependencies: []
# Dependencies will be added here using `sl deps add <url>`
# Example:
# dependencies:
#   - url: git@github.com:org/spec
#     branch: main
#     path: spec.md
#     alias: myspec
#     resolved_commit: abc123def456...
